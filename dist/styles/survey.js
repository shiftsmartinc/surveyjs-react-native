import { StyleSheet } from 'react-native';
export default StyleSheet.create({
    container: {
        // padding: 10,
        paddingTop: 10,
    },
    results: {
        paddingTop: 50,
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    }
});
